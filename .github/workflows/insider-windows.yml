name: insider-windows

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    name: Legacy workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "The legacy insider-windows release workflow is disabled on master during the 112 replay baseline migration."
          echo "Use refactor/112-replay-baseline for P1 replay validation."
